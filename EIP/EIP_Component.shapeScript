// MDG Technology for EIP
// EIP_Component
// www.bobovo.eu

shape main
{
	noshadow=true;

	SetPenWidth(1);
	rectangle(0,0,100,100);  // (x1,y1,x2,y2)
}

shape label
{
	setOrigin("SW",0,0); 
    // define the text for the label
	println("#name#");
}